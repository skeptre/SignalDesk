[project]
name = "signaldesk-backend"
version = "0.0.0"
description = "SignalDesk FastAPI backend (implementation pending)."
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
